---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: kubecit-service
  namespace: wlb965
spec:
  imageRepositoryRef:
    name: kubecit-service
  policy:
    semver:
      range: '>=1.0.0'
