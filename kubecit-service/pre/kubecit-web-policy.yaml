---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: kubecit-web
  namespace: wlb965
spec:
  imageRepositoryRef:
    name: kubecit-web
  policy:
    semver:
      range: '>=1.0.0'
